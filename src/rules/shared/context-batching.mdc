---
description: 上下文分批规则 — 当任务材料较多时，按风险和阶段分批读取、整理和委派，防止上下文膨胀、信息遗漏和子代理漂移。
alwaysApply: true
---

# Context Batching

## 一、核心原则

上下文分批是为了保持信息完整和任务主线稳定，不是为了迎合某个平台的具体工具调用上限。不同平台的工具调用、并发和上下文限制由平台适配层负责；shared 规则只约束跨平台通用行为。

- **必读材料不能跳过**：材料多时分批读取，不得因为省步骤而缺少需求、方案、规则、接口契约或变更证据。
- **先核心后补充**：先读取任务契约、技术方案、AGENTS.md 摘要和变更范围，再读取补充规则、接口契约、历史记录和日志。
- **先收窄再委派**：子代理只接收完成当前职责必需的信息；上下文过大时先摘要和摘录，再启动子代理。
- **保持批次可解释**：分批读取或分批委派时，向用户简要说明当前批次目标和后续批次，不制造“已经完整审查”的错觉。

## 二、分批策略

当上下文材料较多、变更文件较多或需要多个子代理协作时，按以下优先级分批：

1. **核心材料批次**：任务清单、需求/技术方案摘要、AGENTS.md 中与当前阶段相关的项目画像。
2. **契约与规则批次**：接口/数据契约、当前 family-common/profile 规则摘要、测试和验收标准。
3. **证据与验证批次**：git diff、构建/测试/lint 结果、审计线索、性能证据。
4. **写入或委派批次**：在上下文收窄完成后，再执行文件写入、验证命令或子代理委派。

## 三、关键检查点

| 检查点 | 位置 | 分批触发信号 |
|--------|------|--------------|
| 子代理派发前 | B2.2 编排模式 | 当前组需要多类材料，或存在跨组接口依赖 |
| 审计上下文收集 | B3 V4 / audit-context-intake | 变更文件较多，或业务证据分散在多个来源 |
| 验证循环入口 | B3 V1-V5 | 同一轮同时需要读取文件、执行命令、整理状态和委派子代理 |
| 代码审计执行 | audit-reviewer | 高风险文件与普通文件混杂，需要按风险优先级审查 |

## 四、禁止行为

- 禁止把不相关文件或目录粗暴合并读取，只为了减少步骤。
- 禁止把完整技术方案、完整 AGENTS.md、完整 diff 或完整历史讨论直接塞给子代理。
- 禁止在材料不完整时宣称审查、验证或子代理上下文已经完整。
- 禁止把某个平台的具体工具名、调用次数、并发上限写入 shared 规则；这类限制应放在平台适配层或平台专用说明中。

## 五、与平台约束的关系

如果当前平台存在更严格的工具调用、并发、审批或上下文限制，按当前平台约束执行；但平台限制不得成为跳过必读材料或降低治理标准的理由。
