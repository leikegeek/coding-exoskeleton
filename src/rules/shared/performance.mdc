---
description: 通用性能守护规则 - 只约束跨技术栈共性的性能证据、资源边界和验证闭环
alwaysApply: true
---

# Performance Guardrails

- 本规则只定义前后端通用性能门禁；数据库、RPC、响应式渲染等细节由匹配 `techStack` 的 profile 规则补充。
- 性能问题必须先说明证据来源：可复现路径、数据规模、耗时指标、调用次数、渲染次数或资源占用。
- 设计或实现涉及批量数据、循环处理、远程调用、复杂渲染、文件处理时，必须说明规模上限和失败/回滚策略。
- 优先选择低风险、可验证、可回滚的优化；没有证据时只输出排查计划，不直接做结构性重构。
- 性能结论必须闭环到验证方式：构建/测试、压测、浏览器性能面板、日志指标或人工可执行回归路径。
