---
name: coding
displayName: 编码实施
description: 当用户要进行代码开发、缺陷修复、代码重构或按既定方案落地实现时，本技能将启动。强调在遵循现有项目规范的前提下，以 TDD 节奏稳健交付，并在编码过程中增量维护变更记录文档。
triggers: ["开始代码开发", "开始执行开发", "开发这个需求", "修复这个问题", "重构代码"]
autoTrigger: true
version: '1.0.2'
---

# 编码实施

## 核心原则
1. **严格遵循需求与方案**：所有实现必须基于用户确认的需求或设计方案执行；若信息模糊，需先澄清。
2. **编码规范遵从**：严格遵循项目既定的编码风格、命名、格式和工程结构。
3. **避免过度设计**：只实现明确要求的功能，不自行扩展未提及特性。
4. **复用优先**：优先复用现有模式、组件和结构，不轻易新造抽象。
5. **风险意识**：实现后主动检查兼容性、异常处理和潜在影响。
6. **需求编号关联**：所有 commit message 必须关联需求编号。
7. **变更即记录**：每次 commit 后同步更新变更记录文档，确保文档与代码始终一致。

## TDD 工作节奏

每个任务严格遵循 Red-Green-Refactor-Commit-Record-Progress 节奏：

1. **Red**：写单元测试，运行确认失败
2. **Green**：写最小实现代码，运行确认通过
3. **Refactor**：清理代码（如有必要）
4. **Commit**：提交，commit message 格式 `feat(SV-xxxxx): 简短描述`
5. **Record**：更新变更记录文档（见下方"增量变更记录"）
6. **Progress**：更新技术方案文档（`docs/design/SV-xxxxx-tech-design.md`）中的「实施进度」段落，勾选已完成的任务项，更新最后更新时间

## 增量变更记录

每次 commit 后，立即将本次变更追加到 `docs/delivery/SV-xxxxx-changelist.md`。

### 为什么在编码时记录

- 此刻 AI 对变更的理解最准确——刚写完代码，上下文完整
- 避免事后回溯重建，节省 token 消耗
- 为后续代码审查阶段的三方对齐提供基础材料

### 每条记录包含

```markdown
#### T-xxx: {任务描述}

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| path/to/File.java | 新增 | 功能说明 |
| path/to/Other.java | 修改 | 变更说明 |

**接口变更**（如有）：
- `POST /api/xxx` — 新增接口，功能说明

**高风险变更**（如有）：
- `File.java#methodName()` — 已移除，被 NewFile.java 替代
```

### 高风险变更即时标注

以下变更必须在记录时立即标注为高风险：
- 方法级别的删除（整个方法被移除）
- 代码块被注释掉
- 公共接口签名变更（参数、返回值类型改变）

### 文档初始化

编码阶段开始时（B1 实施准备完成后），创建变更记录文档骨架：

```markdown
# 变更清单 SV-xxxxx

> 本文档在编码过程中增量维护，每次 commit 后同步更新。

## 变更记录

（编码过程中逐条追加）

## 需关注：高风险变更

（编码过程中发现高风险变更时追加）
```

## 工作流程
1. 先确认需求、边界和成功标准
2. 阅读相关代码，优先复用现有模式和结构
3. 对复杂任务先给出简短实施计划，再开始修改
4. 按 TDD 节奏实现（含第 5 步 Record）
5. 实现后自查正确性、兼容性、异常处理和可读性
6. 总结修改点、风险点和建议验证方式

## Subagent 并行执行

当作为 Subagent 执行时：
- 携带完整的技术方案上下文和规范约束
- 遵循与主会话相同的 TDD 节奏（含 Record 步骤）
- **不直接写入 `docs/delivery/SV-xxxxx-changelist.md`**（避免多 Subagent 并发写入同一文件导致冲突）
- **不直接更新技术方案文档中的「实施进度」段落**（由主会话在汇总结果时统一更新）
- Record 步骤改为：将变更记录保存为结构化数据（遵循"增量变更记录"中的格式），随任务结果一起返回
- Progress 步骤改为：将任务完成状态随结果一起返回，由主会话统一更新实施进度
- 完成后返回：修改的文件列表、测试结果、commit hash、**本任务的变更记录（结构化文本）**
- 主会话负责将各 Subagent 返回的变更记录按任务顺序汇总追加到变更清单文档

## 输出约定
- 小任务：输出修改点、风险点、验证建议
- 中型任务：输出实施计划、修改摘要、验证结果
- 大任务：如用户需要，再整理为正式 markdown 文档
- 所有任务：变更记录文档始终保持更新

## 交互规则
- 若需求或方案不明确，先提问澄清
- 若项目已有明确规范，以项目规范优先
- 小型任务不强制走完整审查或文档流程，优先快速、准确交付
- 复杂任务再提升审查和验证强度

