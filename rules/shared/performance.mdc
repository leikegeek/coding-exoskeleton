---
description: 通用性能守护规则
alwaysApply: true
---

# Performance Guardrails

- 禁止 N+1 查询；循环内不要发起数据库或远程调用，先批量查询再内存处理。
- 禁止循环单条写入；批量落库优先使用批量接口，如 `bulkInsert`、`bulkUpsert`。
- 大数据量处理必须分页，避免一次性全量 `list()` 或将超大结果集全部加载到内存。
- 大批量任务优先异步分批处理，单批大小按场景控制在合理范围。
- 设计方案时说明数据量级、分页策略、批处理方式和失败补偿，避免默认实现在线上放大性能风险。
