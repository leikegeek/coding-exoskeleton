---
description: Harness 工作模式策略 - 设计模式和编码模式的边界约束
alwaysApply: true
---

# Work Mode Policy

- 任务开始时必须声明当前模式：`设计模式` 或 `编码模式`。
- 设计模式允许读取项目代码与文档，允许写入文档目录，禁止修改项目业务代码。
- 编码模式允许修改项目代码与文档目录，但必须限定目标模块和允许写入路径。
- 未声明模式时，默认按设计模式处理，并优先补齐任务契约后再执行。
- 模式切换前必须说明切换原因、目标和新的写入边界。
