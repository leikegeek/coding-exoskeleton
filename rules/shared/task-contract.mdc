---
description: 任务契约规则 - 每个任务必须建立契约后才能执行
alwaysApply: true
---

# Task Contract

- 每次任务至少包含以下字段：`需求编号`、`模式`、`目标`、`范围`、`允许写入路径`、`禁止项`、`验收标准`。
- 需求编号是必填项，用于全链路关联。编号格式从 `AGENTS.md` 的 `需求编号正则` 字段读取（默认 `SV-\d+`，也支持 JIRA 风格的 `[A-Z]+-\d+`、GitHub 风格的 `#\d+` 等团队自定义格式）。
- 若任务要落代码，必须附带引用的设计文档或明确说明"无先验设计"。
- 未声明范围或写入边界时，不进入实施步骤，只做澄清和补全。
- 验收标准必须可检查，至少包含"产物结构 + 验证方式 + 未验证项说明"。
- 设计模式下只允许读项目代码、写文档，不改项目业务代码。
- 编码模式下必须引用已有设计结论或显式声明无先验设计。

