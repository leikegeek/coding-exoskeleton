---
description: 子代理编排规则 - 定义子代理委派的通用协议、上下文收窄机制、结果回流格式和各子代理的触发条件
alwaysApply: true
---

# Subagent Orchestration

## 一、编排原则

- 子代理由主 Agent 在流水线**到达**对应阶段、且**满足**该子代理的触发条件时，由主流程**自动**调用（用户无需手动@子代理文件）。**「自动」= 主流程在恰当时机发起一次子代理任务**，不表示**并行**、不表示在未经 `commands/code.md` B2 授权时**同时**启动多组「编码子代理」；**组间默认串行**，仅当用户**明确**确认「**允许并行**」时，方可对**无组间依赖**的组**同时**派发（仍须遵守 `code.md` B2.0 / B2.2）。
- 每个子代理有明确的**触发条件**和**绑定阶段**，不跨阶段运行。
- 专职审查类子代理不直接修改主流程的文档和代码，所有产出通过结构化结果返回主 Agent。
- `coding-subagent` 是 B2 编码执行类子代理，是上述规则的唯一例外：它可以在被分配的业务边界和允许写入路径内修改代码，但不得直接写入主流程交付文档。
- 主 Agent 负责收集、整合子代理的结果，并决定后续流程走向。

## 二、上下文收窄协议

每个子代理调用时，主 Agent 必须遵循上下文收窄协议：

### 2.1 最小信息集原则

只传递子代理完成任务所必需的信息，具体范围由各 Agent 文件中的「上下文边界 → 必须接收」章节定义。

编码子代理的上下文必须优先使用摘要和摘录：任务清单、代码实施位置、接口契约、必要规则摘要。禁止为了方便直接传完整技术方案、完整 `AGENTS.md`、完整 git diff 或完整历史讨论。

### 2.2 信息隔离原则

不传递以下类型的信息给子代理：
- 其他子代理的历史对话和中间产出
- 与当前子代理职责无关的阶段产物（如编码阶段的产物不传给 A2 阶段的 architect）
- 用户的个人偏好和非结构化的口头讨论内容
- Hooks 审计日志（除非子代理职责明确要求）

具体的禁止项由各 Agent 文件中的「上下文边界 → 禁止接收」章节定义。

### 2.3 收窄执行检查

主 Agent 在委派子代理前，需确认：
1. 传递的上下文是否符合该子代理的「必须接收」清单——缺项不委派
2. 传递的上下文是否包含「禁止接收」的内容——有则剔除后再委派

## 三、结果回流格式

所有子代理的产出统一为以下三段式结构：

```markdown
## [子代理名称] 结论

- **结论**：（一句话结论，如"通过 / 需修复 / 需人工介入"）
- **补充指标**：（子代理特有的量化指标）

## 问题列表

### Critical（阻断性问题）
- [定位信息] 问题描述 → 修复建议

### Warning（建议修复）
- [定位信息] 问题描述 → 修复建议

### Info（可选优化）
- [定位信息] 问题描述 → 改进方向

## 建议
- （全局性的改进建议）
```

主 Agent 收到结果后：
- Critical 问题阻断当前流程，必须修复后重新执行
- Warning 问题记录到对应的交付文档，不阻断流程
- Info 问题按需采纳

## 四、子代理触发条件与调用时机

### 4.1 architect（架构决策审查）

| 属性 | 值 |
|------|-----|
| 绑定阶段 | A2（技术方案设计） |
| 触发条件 | 技术方案涉及以下任一场景时自动触发：新增模块、引入新技术组件、跨模块数据流变更、数据模型重构 |
| 调用时机 | `design-review` skill 自评审完成后、用户确认方案前 |
| 前置依赖 | 技术方案文档已生成、`AGENTS.md` 可读取 |

### 4.2 tdd-guide（TDD 引导）

| 属性 | 值 |
|------|-----|
| 绑定阶段 | B2（编码实施） |
| 触发条件 | 每个任务的 Red-Green-Commit 周期结束时自动检查；检测到以下情况时立即介入：实现代码 commit 中不包含对应测试文件、测试文件 commit 时间晚于实现代码 |
| 调用时机 | 每个任务 commit 后、下一个任务开始前 |
| 前置依赖 | 当前任务已完成 commit |

### 4.3 build-error-resolver（构建错误修复）

| 属性 | 值 |
|------|-----|
| 绑定阶段 | B3（验证循环 V1） |
| 触发条件 | V1 构建验证返回 ❌ 时自动触发 |
| 调用时机 | V1 失败后、进入修复流程前 |
| 前置依赖 | 构建命令的完整错误输出 |

### 4.4 security-reviewer（安全审计）

| 属性 | 值 |
|------|-----|
| 绑定阶段 | B3（验证循环收口之后、进入 B4 之前） |
| 触发条件 | `verification-loop` 完成 V1–V5 且**综合评判为 PASS** 之后自动触发；如果变更涉及以下内容则**必须**执行：接口层代码变更、权限/认证相关代码变更、数据库操作代码变更、文件上传/下载功能 |
| 调用时机 | 五维验证循环 **PASS 后**、**B4 交付展示前**（不与 V1–V5 中某一维竞序，避免与 `verification-loop` 的 V4→V5 顺序冲突） |
| 前置依赖 | V1–V5 已按 `verification-loop` 执行完毕且综合可进入交付前评审 |

### 4.5 doc-updater（文档完整性）

| 属性 | 值 |
|------|-----|
| 绑定阶段 | B4（交付） |
| 触发条件 | B3 中 V1–V5 **综合 PASS** 且 `security-reviewer` **可交付**（无未解决 Critical 或已闭环）后、展示交付物前自动触发 |
| 调用时机 | 安全审计完成后、B4 交付物展示前 |
| 前置依赖 | 三类正式交付文档已生成（变更清单、技术参考文档、代码评审报告）；代码评审报告已吸收 V1-V5 验证摘要与安全审计结论 |

### 4.6 coding-subagent（编码执行）

| 属性 | 值 |
|------|-----|
| 绑定阶段 | B2（编码实施） |
| 触发条件 | B1 任务拆解与业务边界归组完成后，**编码任务分组数 ≥ 2** 时进入**编排模式**（与 `commands/code.md` B2.0 一致），主 Agent 转为编排者角色；分组数 < 2 为直接执行模式 |
| 调用时机 | B1 完成、业务边界归组完成、用户确认派发方案后，按派发顺序逐组派发 |
| 前置依赖 | 任务清单已生成、业务边界归组已完成、派发方案已获用户确认、变更记录文档骨架已初始化 |
| 执行方式 | **默认串行**——前一组验收通过后再派发下一组；仅当用户显式确认"允许并行"时才可同时派发无依赖的多组 |

**编排模式的核心约束**：

- 主 Agent 进入编排模式后，其职责限定为：拆分、派发、验收、收敛。**禁止直接编写业务代码**。
- **默认串行**：在用户未明确确认「**允许并行**」前，**禁止**同时启动两个及以上编码子代理调用；每次只派发一组，验收通过后再派发下一组。
- 每组派发后，主 Agent 等待子代理返回结构化结果，执行验收（检查完成状态、审核假设决策、确认测试通过），验收通过后再派发下一组。
- 如果子代理执行失败，主 Agent 决策"重新派发同组"或"退出编排模式由主 Agent 接管"。退出编排模式必须向用户说明原因。
- 所有组完成并验收后，主 Agent 执行收敛：合并变更记录到 `changelist.md`、更新实施进度、运行集成验证。
- 编码子代理不得对业务关键缺口自行假设后继续实现；涉及业务规则、数据口径、权限、安全、接口契约、兼容迁移的缺口，必须返回主 Agent 澄清。

**结果回流格式**：编码子代理的返回格式不使用通用三段式结构，而是使用专用的「编码执行结果」结构（定义在 `coding-subagent` agent 文件中），包含任务完成状态、变更记录、测试结果、接口契约和假设决策记录。

## 五、子代理与主 Agent 的职责边界

### 5.1 通用职责边界（适用于专职审查类子代理）

| 职责 | 主 Agent | 子代理 |
|------|---------|--------|
| 流程编排 | ✅ 决定何时调用哪个子代理 | ❌ 不干预流程走向 |
| 上下文准备 | ✅ 收窄上下文后传递 | ❌ 不主动获取额外信息 |
| 执行检查 | ❌ 不重复子代理的专项检查 | ✅ 在专职领域内执行深度检查 |
| 结果整合 | ✅ 收集所有子代理结果并决策 | ❌ 不直接修改文档或代码 |
| 用户沟通 | ✅ 汇总后向用户报告 | ❌ 不直接与用户交互 |
| 修复执行 | ✅ 根据子代理建议执行修复 | ❌ 只给建议不执行 |

### 5.2 B2 编排模式职责边界（适用于编码执行子代理）

当 B2 进入编排模式后，主 Agent 的角色从"执行者"转为"编排者"：

| 职责 | 主 Agent（编排者） | 编码子代理 |
|------|-------------------|-----------|
| 任务拆分与归组 | ✅ 负责 | ❌ 不参与 |
| 派发决策 | ✅ 负责（含串行/并行决策） | ❌ 不参与 |
| 业务代码编写 | **❌ 禁止**（编排模式下不写业务代码） | ✅ 负责 |
| TDD 节奏执行 | ❌ 委派子代理执行 | ✅ 负责 |
| 验收子代理产出 | ✅ 负责（检查状态、审核假设、确认测试） | ❌ 返回结构化结果 |
| 变更记录汇总 | ✅ 负责（将各组结果合并到 changelist.md） | ❌ 返回本组变更记录，不直接写文件 |
| 实施进度更新 | ✅ 负责（每组验收后更新） | ❌ 不更新 |
| 集成验证 | ✅ 负责（所有组完成后） | ❌ 不参与 |
| 异常处理 | ✅ 负责（决策重派或接管） | ❌ 报告失败原因 |

## 六、异常处理

- 子代理执行超时或失败时，主 Agent 记录异常原因，跳过该子代理继续流程，并在交付物中标注"未完成 [子代理名称] 检查"。
- 多个子代理结论冲突时（如 security-reviewer 认为某实现有风险但 code-reviewer 认为合理），以更严格的结论为准，由主 Agent 汇总后交用户决策。
- 子代理返回的结果格式不符合三段式结构时，主 Agent 尝试提取关键信息，并在报告中标注"结果格式异常，已人工提取"。
