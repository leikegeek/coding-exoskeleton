---
description: Vue 3 项目规则：SFC、Composition API、代码生成 API、Element Plus 包装组件
globs:
  - "**/*.vue"
  - "src/{views,components,stores,routes,composables,common,api,team1api,apiGenerate,commonapi,couponapi,customapi}/**/*.ts"
alwaysApply: false
---

# Vue 3 Project

- 仅当 `AGENTS.md` 的 `techStack` 为 `frontend-vue3` 时使用本规则；其它项目跳过。
- Vue 3 项目默认使用 SFC + `<script setup lang="ts">` + Composition API，优先沿用 Pinia、Vue Router、Element Plus 和现有封装组件。
- 不手改代码生成 API 目录；新增或更新接口优先通过项目 swagger/codegen 脚本生成，确需临时补充时必须标注后续生成覆盖策略。
- Element Plus 包装组件要尽量保持原组件 API 兼容；程序化归一化、格式转换、默认值同步不得误触发用户语义的 `change`。
- `v-model` 组件必须遵循 `modelValue` / `update:modelValue` 协议；不要直接修改 props。
- 模板组件名使用 PascalCase，自定义事件使用 kebab-case；避免大范围格式化无关模板。
